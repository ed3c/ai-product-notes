# CopilotKit OpenBot — Open Asset Verification Note

- Primary repo: https://github.com/CopilotKit/OpenBot
- Verified commit: `3c5cc825b94ff8458945d69016b8a405fc0ab1da`
- Commit timestamp: 2026-08-23T22:40:19Z
- License gate: PASS for repository code / MIT
- Product maturity: Alpha
- Formal asset score: 8.0

## 繁體中文

### 技術概況

OpenBot 是 CopilotKit 的 open-source AI coworker platform。每個 Bot 可以取得獨立 computer/container、browser profile、workspace、files 與被授權的 tools；browser/file/MCP/component actions 會先經過中央 gateway 的 policy 判斷並寫入 audit record，再執行或拒絕。Agent endpoint 透過 AG-UI 接入，因此可與 LangGraph、Mastra、CrewAI、Pydantic AI、Google ADK 或自訂 agent 搭配。

### Freshness 與 production evidence

本次 pinned `main` commit 於 2026-08-23T22:40:19Z，修正 live-screen reconnect 時舊 socket 關閉會誤停新 viewer 的 race condition。這是 fresh engineering activity，不被誤寫成 OpenBot 首次 launch；初始 open-source alpha 早於本監控窗口。

Upstream README 明確標示 Alpha / active development。近期 repository issues 與 fixes 顯示團隊正在處理 authorization、private-address checks、credential rotation、snapshot refs、computer isolation 等 production-boundary 問題。因此 Production score 保守給 6。

### License evidence

Pinned commit 的 root `LICENSE` 為 MIT。PASS 只涵蓋 repository code。

不推定下列資產權利：

- `assets.llm_models`: external model providers；沒有 bundled model weights rights。
- `assets.data`: company/customer documents、vectors、browser state、connector data 不屬於 MIT reusable dataset。
- `assets.trajectories`: audit/session/runtime histories 是 deployment artifacts，不視為可重用 licensed trajectory corpus。
- CopilotKit Intelligence project/license、managed service、third-party connectors 與 provider credentials 需獨立遵守各自條款。

### 技術落地價值

適合作為以下 architecture reference：

- per-agent computer isolation；
- CEL-style fail-closed action policy；
- pre-action decision + post-action audit；
- governed MCP；
- write-only encrypted credentials；
- human takeover / hand-back；
- AG-UI framework-neutral agent endpoint；
- local/self-hosted enterprise coworker surface。

### 風險

Alpha maturity、Docker socket/supervisor boundary、identity provider setup、browser/session isolation、credential rotation、third-party website terms 與 policy false-negative/false-positive 都需要獨立 production hardening。MIT code license 不代表整個 hosted or connected system 可以無條件商用。

## English

OpenBot is an open-source AI coworker platform from CopilotKit. Each Bot can receive its own computer/container, browser profile, workspace, files, and granted tools. Browser, file, MCP, and component actions pass through a central policy gateway that records a decision before acting or refusing. Agents connect through AG-UI, keeping the surface framework-neutral.

The pinned revision `3c5cc825b94ff8458945d69016b8a405fc0ab1da` is dated 2026-08-23T22:40:19Z. The current code is still explicitly alpha and under active development, so production maturity is scored conservatively.

The repository code is MIT at the pinned revision. This PASS does not include external model weights, CopilotKit Intelligence service terms, customer/company data, browser state, third-party connectors, or runtime trajectories.

Internal score: Hackathon MVP 9, Business leverage 7, Research 8, Production 6, Stack compatibility 10, average 8.0.