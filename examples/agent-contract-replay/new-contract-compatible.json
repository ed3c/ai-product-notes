{
  "schema_version": "agent-contract.v2-compatible",
  "tools": [
    {
      "name": "search_skill",
      "required": ["query"],
      "properties": {
        "query": {"type": "string"},
        "mode": {"type": "string", "enum": ["semantic", "exact", "hybrid"]},
        "registry": {"type": "string"}
      }
    },
    {
      "name": "run_assertion",
      "required": ["command"],
      "properties": {
        "command": {"type": "string"},
        "timeout_seconds": {"type": "integer"}
      }
    },
    {
      "name": "legacy_fetch",
      "required": ["url"],
      "properties": {
        "url": {"type": "string"}
      }
    }
  ]
}
