{
  "schema_version": "agent-contract.v1",
  "tools": [
    {
      "name": "search_skill",
      "required": ["query"],
      "properties": {
        "query": {"type": "string"},
        "mode": {"type": "string", "enum": ["semantic", "exact"]}
      }
    },
    {
      "name": "run_assertion",
      "required": ["command"],
      "properties": {
        "command": {"type": "string"}
      }
    },
    {
      "name": "legacy_fetch",
      "required": ["url"],
      "properties": {
        "url": {"type": "string"}
      }
    }
  ]
}
