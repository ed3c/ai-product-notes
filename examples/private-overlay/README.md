# Private overlay example

Copy `config/private-portfolio.example.json` to the Git-ignored `config/private-portfolio.json` and replace only the sanitized capability envelope. Do not add repository names, paths, URLs, source code, raw traces, customer data, account identities or credentials. The compiler rejects forbidden keys and URL/path/account-like values.
